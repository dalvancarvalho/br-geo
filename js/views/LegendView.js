//Legend View
export class LegendView {
  // Public Property
  constructor() {
    // Public Properties
    // Private Properties
  }

  // Public Methods
  update(rootElement, map, mapData) {
    // Updates the ...

    console.log(rootElement);
    console.log(map);
    console.log(mapData);
  }

  // Private Methods
  _clear(rootElement) {
    // ...
  }

  _render(rootElement, param) {
    // Inserts the template inside the root element.
  }

  _template(param) {
    // Returns a string with ...
  }

  _getRenderedElements(rootElement) {
    // Stores some elements in properties for further use ...
  }

  _addEventListeners(rootElement) {
    // ...
  }
}
